console.log("yeeer");

var follow_link = $(".<%= dom_id(@follow_request.recipient) %>_new_follow_request_form");
var unfollow_link = $("<%= j(render("follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient)) %>");
follow_link.replaceWith(unfollow_link);
